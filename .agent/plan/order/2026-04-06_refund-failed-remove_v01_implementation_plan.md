# 退款失败状态移除实施计划

## 目标

- 将 `FAILED` 从当前订单域退款状态模型中移除
- 避免继续给人“订单域已支持退款失败终态”的错误预期

## 实施步骤

1. 从 `RefundStatusEnum` 中移除 `FAILED`
2. 更新 `o_refund_apply.refund_status` 的设计 SQL 注释
3. 更新退款两阶段设计文档，明确发起退款失败不在订单域状态模型中落终态

## 边界

- 本次不修改历史 plan 归档内容
- 支付中心退款发起失败仍留在调用支付中心时自行处理
