# 实施计划

## 任务

抖音 SPI 验签链路只保留新签名 `x-life-sign`，移除旧签名 `sign` / MD5 兼容逻辑。

## 实施步骤

1. 检查 `DouyinSignVerifyUtil` 中新旧签名的实现与注释。
2. 删除旧签名相关方法、注释和示例代码。
3. 修改 `DouyinSpiSignatureService`，仅执行新签名验签。
4. 检查抖音订单链路中是否仍存在旧签名残留引用。
5. 归档本次实施计划与工作总结。

## 风险与关注点

- 变更后不再兼容 URL 中的旧 `sign` 签名。
- 当前仍保留原始 body 读取方式；本次只做签名协议收口，不处理大 body 的内存保护。
