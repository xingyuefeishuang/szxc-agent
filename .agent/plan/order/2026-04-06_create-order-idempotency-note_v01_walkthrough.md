# createOrder 幂等前置与备注补充工作总结

## 本次改动
- 在 `OrderServiceImpl.createOrder(...)` 开头增加外部单幂等查询：
  - 按 `channelCode + channelOrderNo` 查询已存在订单
  - 命中时直接返回既有 `orderNo`
- 在方法内补充备注：
  - 外部渠道单优先在核心层按外部单号复用
  - 内部单通用幂等暂由网关统一处理
- 在主单落库前补充库存域接入前的顺序风险说明

## 结果
- 外部渠道重复创单请求不再一律生成新订单号。
- 内部单幂等边界在代码中被显式标注，避免后续误解为核心层已统一兜底。

## 未覆盖事项
- 该方案仍未解决并发下的数据库最终真相问题。
- 库存校验与预占仍未落地。
- 未补自动化测试，本次结论基于静态代码修改。
